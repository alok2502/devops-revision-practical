apiVersion: v1
kind: Service
metadata:
  name: web-svc
spec:
  selector:
    app: webserver      # <-- the deployment's pods are labeled app=web, NOT app=webserver
  ports:
  - port: 80
    targetPort: 80
