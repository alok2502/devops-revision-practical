apiVersion: v1
kind: Pod
metadata:
  name: toobig
spec:
  containers:
  - name: app
    image: nginx:1.27.0
    resources:
      requests:
        memory: "100Gi"      # no node has 100Gi — unschedulable
