apiVersion: v1
kind: Pod
metadata:
  name: broken4
spec:
  containers:
  - name: app
    image: polinux/stress
    resources:
      requests:
        memory: "32Mi"
      limits:
        memory: "64Mi"
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "150M", "--vm-hang", "1"]
