FROM python:3.12-slim
# create a dedicated non-root user
RUN useradd --create-home --shell /usr/sbin/nologin appuser
WORKDIR /app
COPY app.py .
# drop to the non-root user for everything after this
USER appuser
CMD ["python", "app.py"]
